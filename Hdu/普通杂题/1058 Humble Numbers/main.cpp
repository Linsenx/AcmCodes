#include <bits/stdc++.h>
using namespace std;

int count;
vector<int>hums;

void getHums()
{  
  int a1, a2, a3, a4;
  hums.push(1);
  hums.push(2);
  hums.push(3);
  hums.push(5);
  hums.push(7);
  
}

int main()
{
  ios::sync_with_stdio(false);
  #ifdef ONLINE_JUDGE
  #else
    freopen("in.txt","r",stdin);
  #endif

  return 0;
}
