#include<iostream>
#include<math.h>
using namespace std;

int main()
{
  //ios::sync_with_stdio(false);
  freopen("in.txt","r",stdin);

  int i, j, len;
  int a, b;
  string bs;
  while (cin >> len) {
    cin >> bs;
    if (sum % 5 == 0) cout << "Yes!" << endl;
    else cout << "No!" << endl;
  }


  return 0;
}
